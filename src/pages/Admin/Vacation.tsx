import { Box, Typography } from '@mui/material';

export const Vacation = () => {
  return (
    <Box sx={{ display: 'flex', flexGrow: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Typography variant="h2">Vacation Page</Typography>
    </Box>
  );
};
