import { Box, Typography } from '@mui/material';

export const Tasks = () => {
  return (
    <Box sx={{ display: 'flex', flexGrow: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Typography variant="h2">Tasks Page</Typography>
    </Box>
  );
};
