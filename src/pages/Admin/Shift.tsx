import { Box, Typography } from '@mui/material';

export const Shift = () => {
  return (
    <Box sx={{ display: 'flex', flexGrow: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Typography variant="h2">Shift Page</Typography>
    </Box>
  );
};
