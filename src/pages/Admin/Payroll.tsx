import { Box, Typography } from '@mui/material';

export const Payroll = () => {
  return (
    <Box sx={{ display: 'flex', flexGrow: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Typography variant="h2">Payroll Page</Typography>
    </Box>
  );
};
